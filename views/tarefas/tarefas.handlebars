<!DOCTYPE html>
<html lang="en">
    <head>
        {{>head}}
        <title>Minhas Tarefas</title>
    </head>
    <body>
        <h1>Minhas Tarefas</h1>
        <div class="container">
            <a href="/tarefas/adicionarTarefa">
                <button class="btn btn-outline-danger mb-2">Adicionar Tarefa</button>    
            </a>
            <ol class="list-group list-group-numbered">
                {{#each tarefas}}
                    <li class="list-group-item d-flex justify-content-between align-items-start">
                        <div class="ms-2 me-auto">
                            <div class="fw-bold">{{titulo}}</div>
                                {{descricao}}
                        </div>
                        <button class="button">Já feita</button>
                        <button class="button">Editar</button>


                        <form action="/tarefas/excluir/{{_id}}" method="post">
                            <button type="submit" class="button submit">Excluir</button>
                        </form>
        
                    </li>
                {{/each}}
            </ol>
        </div>
    </body>
</html>